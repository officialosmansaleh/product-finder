{
  "name": "product-finder-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Mobile wrapper (Android/iOS) for Product Finder backend",
  "scripts": {
    "cap:init": "npx cap init ProductFinder com.productfinder.mobile --web-dir=www",
    "cap:sync": "npx cap sync",
    "android": "npx cap open android",
    "ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^7.1.0",
    "@capacitor/core": "^7.1.0",
    "@capacitor/ios": "^7.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.1.0"
  }
}
